global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "hse-backend"
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials: local-dev-metrics-token
    static_configs:
      - targets: ["backend:8000"]
